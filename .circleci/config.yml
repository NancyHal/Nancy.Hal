# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  build:
      docker:
        - image:  microoft/dotnet 
      steps:
            - checkout : git clone https://github.com/GlobalX/Nancy.Hal.git
	    - checkout : git checkout -b feature/add-circle-ci origin/feature/add-circle-ci
      	    - run: dotnet restore
      	    - run: dotnet build
